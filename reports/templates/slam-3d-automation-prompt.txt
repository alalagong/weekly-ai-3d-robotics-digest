每次运行先读 `.cache/slam-3d-state.json`，无状态再读 `reports/index.html`/最新报告；覆盖范围为上次 `coverage_end` 后到当前运行时间，间隔>7天按实际间隔覆盖，不截断为“最近7天”。成功后更新状态：`run_at`、`coverage_start/end`、`report_path`。

先打开并遵循 `reports/templates/slam-3d-weekly-template.html`；版式参考 `reports/2026-06-14_2026-06-21_MMD-SLAM-SGM-SLAM-MoonSplat.html`。HTML 输出到 `reports/YYYY-MM-DD_YYYY-MM-DD_关键词1-关键词2.html`，并更新 `reports/index.html`。

范围：核心 SLAM、VIO、SfM/Structure from Motion、FeedForward 3D Reconstruction；扩展只收与 3DGS、机器人、导航、建图、定位、具身智能、世界模型直接相关内容。目标是科研/工程判断，不做新闻聚合。没有读到的内容必须标明证据等级，不能把 abstract/README 改写成深度分析，不能用无证据的“显著提升/优秀/很有潜力”。

论文：从 arXiv 搜覆盖期新论文，优先 cs.CV/cs.RO/cs.AI/cs.LG/eess.IV/eess.SY；关键词含 SLAM、VIO、SfM、3D reconstruction、feed-forward 3D、3DGS、robotics、navigation、mapping、localization、embodied AI、world model。arXiv API/Invoke-WebRequest 不稳时换 advanced search、网页搜索或 web search。

Deep-read Papers：少量高质量优先。尽量读完整 PDF/HTML，覆盖 Intro、Method、Experiments、Ablation/Analysis、Limitations/Conclusion、必要 Appendix。按模板列出：解决的问题、方法/结构、insight 与数学知识、实验与效果、复现与启发、阅读证据。实验要写清数据集/指标/baseline/相对结果。

Supplemental Tracking Papers：也必须至少读摘要；每条按模板压缩列出：方向关系、解决的问题、方法/结构、insight 与数学知识、实验、证据级别与后续动作。若只读摘要/元数据，明确写“摘要级/元数据级，未深读全文”，不得作为深度结论来源。

系统图：Deep-read/Top papers 若有 overview/framework/pipeline/architecture/flowchart 图，优先截取并视觉确认；只收 caption/section 明确指向总览/流程的图。放入 Deep-read 表的“系统/框架图”列，用可点击缩略图链接原图；Supplemental 不嵌图，不单建 System Figures 区。

Repo：从 GitHub 搜覆盖期内新出现/升温项目，可用 search/trending/topic/starred。重点 repo 不能只看 stars/README；尽量查 README、docs、examples、入口、核心模块、配置、训练/推理/运行脚本、近期 commit/release/issue，并列实际文件/目录。总结目标、能力、路线、结构、模块协作、依赖/硬件、stars/forks、成熟度、领域关系；区分声称能力与代码/示例/实验可验证能力，证据不足写“未充分验证”。

报告必须包含模板中的所有 section：header 统计 tiles、Executive Summary、Deep-read Papers、Repo Deep Dives、Quantitative Experiment Highlights、Supplemental Tracking Papers、Trends & Insights、Evidence & Reading Coverage、值得跟进事项、原始链接列表。Executive Summary 只能压缩后文已验证结论。

Git：优先 Windows Git `C:\Users\gongy\AppData\Local\Programs\Git\cmd\git.exe`；若不可用再用 `wsl -d Ubuntu-24 -- git -C /mnt/c/Users/gongy/Documents/每周行业进展-fromAI ...`。只提交本次报告相关文件：新报告、index、assets、状态文件；`.cache` 若被 ignore，用 `git add -u .cache/slam-3d-state.json` 或 `git add -f`。提交前查暂存区，不提交无关用户改动。commit：`更新 YYYY-MM-DD 至 YYYY-MM-DD SLAM与3D视觉周报`；push origin/main，失败不强推，说明原因和人工步骤。

最终回复：报告路径、覆盖时间、深读论文数、repo 深读数、摘要/元数据级条目数、无法访问/证据不足/需登录来源、commit hash、push 结果。
